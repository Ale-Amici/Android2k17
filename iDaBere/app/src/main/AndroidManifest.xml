<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unitn.disi.lpsmt.idabere">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".SearchBarActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.search_bar_searchable"
                android:resource="@xml/search_bar_searchable" />
        </activity>
        <activity
            android:name=".ItemInfoActivity"
            android:label="@string/title_item_info">

            <!-- TODO aggiungere android:parentActivityName="com.example.myfirstapp.MainActivity" -->
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY" />
            <!-- TODO aggiungere quando integrato la parent activity android:value="com.example.myfirstapp.MainActivity" -->
        </activity>
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".QrCodeScannerActivity"
            android:label="@string/title_qr_code_scanner_login">

            <!-- TODO Aggiungere android:parentActivityName="com.example.myfirstapp.MainActivity" -->
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY" />
            <!-- TODO Aggiungere android:value="com.example.myfirstapp.MainActivity" -->
        </activity>
        <activity android:name=".GpsLoadingActivity" />
        <activity android:name=".PaymentTypeActivity"></activity>
    </application>

</manifest>